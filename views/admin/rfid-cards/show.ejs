<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>RFID Card Details</title>
</head>
<body>
  <div class="container mt-4">
    <div class="row">
      <div class="col-md-8 offset-md-2">
        <div class="card">
          <div class="card-header d-flex justify-content-between align-items-center">
            <h2 class="mb-0">RFID Card Details</h2>
            <div>
              <a href="/admin/rfid-cards/<%= card.id %>/edit" class="btn btn-warning" data-modal="true">
                <i class="fas fa-edit"></i> Edit
              </a>
              <form action="/admin/rfid-cards/<%= card.id %>?_method=DELETE" method="POST" class="d-inline">
                <button type="submit" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this RFID card?')">
                  <i class="fas fa-trash"></i> Delete
                </button>
              </form>
            </div>
          </div>
          <div class="card-body">
            <table class="table table-bordered">
              <tbody>
                <tr>
                  <th style="width: 200px;">Card UID</th>
                  <td><%= card.card_uid %></td>
                </tr>
                <tr>
                  <th>Status</th>
                  <td>
                    <span class="badge <%= card.is_active ? 'bg-success' : 'bg-danger' %>">
                      <%= card.is_active ? 'Active' : 'Inactive' %>
                    </span>
                  </td>
                </tr>
                <tr>
                  <th>Issued At</th>
                  <td><%= card.issued_at ? new Date(card.issued_at).toLocaleString() : 'Not specified' %></td>
                </tr>
                <tr>
                  <th>Last Used</th>
                  <td><%= card.last_used ? new Date(card.last_used).toLocaleString() : 'Never used' %></td>
                </tr>
                <tr>
                  <th>Assigned User</th>
                  <td>
                    <% if (card.user_id) { %>
                      <a href="/admin/users/<%= card.user_id %>" data-modal="true">View User</a>
                    <% } else { %>
                      Not assigned
                    <% } %>
                  </td>
                </tr>
                <tr>
                  <th>Created At</th>
                  <td><%= new Date(card.createdAt).toLocaleString() %></td>
                </tr>
                <tr>
                  <th>Updated At</th>
                  <td><%= new Date(card.updatedAt).toLocaleString() %></td>
                </tr>
              </tbody>
            </table>
          </div>
          <div class="card-footer">
            <a href="/admin/rfid-cards" class="btn btn-secondary">
              <i class="fas fa-arrow-left"></i> Back to RFID Cards
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html> 