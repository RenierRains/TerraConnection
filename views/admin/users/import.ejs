<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/app.css">
  <style>
    .import-container {
      max-width: 500px;
      margin: 2rem auto;
      padding: 2rem;
      background-color: #f8f9fa;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }
    .import-container h2 {
      margin-bottom: 1rem;
    }
  </style>
</head>
<body>
  <div class="import-container">
    <h2>Import Users</h2>
    <p>Select a CSV or Excel (.xlsx) file to import user data. The first row should contain headers matching the fields: <strong>first_name, last_name, email, role, school_id</strong>.</p>
    <form action="/admin/users/import" method="POST" enctype="multipart/form-data">
      <div class="form-group">
        <input type="file" name="importFile" class="form-control" accept=".csv,.xlsx" required>
      </div>
      <button type="submit" class="btn btn-primary mt-3">Import Users</button>
      <a href="/admin/users" class="btn btn-secondary mt-3">Cancel</a>
    </form>
  </div>
</body>
</html>
