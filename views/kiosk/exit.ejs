<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Visitor Exit - TerraConnection</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            margin: 0;
            padding: 0;
            min-height: 100vh;
            background: linear-gradient(135deg, #6a0dad, #4b0082, #8a2be2, #9400d3);
            background-size: 400% 400%;
            animation: gradientBG 15s ease infinite;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        @keyframes gradientBG {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .kiosk-container {
            max-width: 800px;
            margin: 0 auto;
            padding: 30px 20px;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .kiosk-card {
            background: rgba(255, 255, 255, 0.96);
            border-radius: 24px;
            box-shadow: 0 18px 45px rgba(0, 0, 0, 0.18);
            overflow: hidden;
            width: 100%;
        }

        .kiosk-header {
            background: linear-gradient(135deg, #6a0dad, #8a2be2);
            color: #fff;
            padding: 40px 35px;
            text-align: center;
        }

        .kiosk-header h1 {
            font-weight: 700;
            font-size: 2.5rem;
            margin-bottom: 10px;
        }

        .kiosk-body {
            padding: 40px 35px 20px;
        }

        .scan-panel {
            border: 2px dashed rgba(106, 13, 173, 0.35);
            border-radius: 16px;
            padding: 25px;
            text-align: center;
            background: rgba(250, 248, 255, 0.7);
        }

        .scan-panel i {
            font-size: 3.5rem;
            color: #6a0dad;
            margin-bottom: 10px;
        }

        .form-control, .form-control:focus {
            border-radius: 14px;
            border: 2px solid rgba(138, 43, 226, 0.25);
            box-shadow: none;
        }

        .form-control:focus {
            border-color: rgba(106, 13, 173, 0.6);
        }

        .form-footer {
            padding: 25px 35px 35px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 15px;
            border-top: 1px solid rgba(106, 13, 173, 0.1);
            background: rgba(250, 248, 255, 0.8);
        }

        .btn-kiosk {
            border-radius: 14px;
            padding: 12px 22px;
            font-weight: 600;
        }

        .spinner-border {
            width: 2.5rem;
            height: 2.5rem;
        }

        @media (max-width: 768px) {
            .kiosk-body {
                padding: 30px 20px;
            }

            .form-footer {
                flex-direction: column;
                align-items: stretch;
            }
        }
    </style>
</head>
<body>
    <div class="kiosk-container">
        <div class="kiosk-card">
            <div class="kiosk-header">
                <h1><i class="fas fa-door-open me-2"></i>Visitor Exit</h1>
                <p>Scan the visitor pass to end your visit. Thank you for coming!</p>
            </div>
            <div class="kiosk-body">
                <div id="statusMessage"></div>
                <div class="scan-panel mb-4">
                    <i class="fas fa-id-card"></i>
                    <h3 class="mt-2">Scan Visitor Pass</h3>
                    <p class="text-muted mb-0">Hold the visitor pass near the scanner. The form will submit automatically.</p>
                </div>

                <form id="exitForm" autocomplete="off" spellcheck="false">
                    <div class="mb-4">
                        <label for="rfidCardUid" class="form-label text-muted">Visitor Pass (RFID)</label>
                        <input type="text" id="rfidCardUid" class="form-control form-control-lg text-center" placeholder="Tap visitor pass here" autocomplete="off" required>
                        <div id="scanStatus" class="small text-muted mt-2 text-center"></div>
                    </div>
                    <div class="d-flex gap-2">
                        <button type="button" class="btn btn-outline-secondary btn-kiosk" onclick="window.location.href='/kiosk/'">
                            <i class="fas fa-arrow-left"></i> Main Menu
                        </button>
                        <button type="submit" class="btn btn-success btn-kiosk flex-grow-1" id="submitBtn">
                            <i class="fas fa-check-circle"></i> Confirm Exit
                        </button>
                        <button type="button" class="btn btn-outline-secondary btn-kiosk" id="clearPassBtn">
                            <i class="fas fa-eraser"></i>
                        </button>
                    </div>
                </form>
            </div>
            <div class="form-footer">
                <div class="d-flex align-items-center gap-2">
                    <div id="loadingSpinner" class="spinner-border text-primary" role="status" style="display: none;">
                        <span class="visually-hidden">Processing...</span>
                    </div>
                    <span class="text-muted small">Forgot your pass? Please contact the lobby staff.</span>
                </div>
                <a href="/kiosk/entry" class="btn btn-outline-primary btn-kiosk">
                    <i class="fas fa-address-card"></i> New Visitor
                </a>
            </div>
        </div>
    </div>

    <script>
        class VisitorExitProcessor {
            constructor() {
                this.form = document.getElementById('exitForm');
                this.passInput = document.getElementById('rfidCardUid');
                this.clearPassBtn = document.getElementById('clearPassBtn');
                this.scanStatus = document.getElementById('scanStatus');
                this.statusMessage = document.getElementById('statusMessage');
                this.loadingSpinner = document.getElementById('loadingSpinner');
                this.submitBtn = document.getElementById('submitBtn');

                this.attachEvents();
                this.passInput.focus();
            }

            attachEvents() {
                this.form.addEventListener('submit', (event) => {
                    event.preventDefault();
                    this.processExit();
                });

                this.passInput.addEventListener('keydown', (event) => {
                    if (event.key === 'Enter') {
                        event.preventDefault();
                        this.processExit();
                    }
                });

                this.passInput.addEventListener('input', () => {
                    this.passInput.classList.remove('is-invalid');
                    this.updateScanStatus('');
                });

                this.clearPassBtn.addEventListener('click', () => {
                    this.passInput.value = '';
                    this.passInput.classList.remove('is-valid', 'is-invalid');
                    this.updateScanStatus('');
                    this.passInput.focus();
                });
            }

            normalizeCard(value) {
                return value.trim().replace(/[^0-9a-z]/gi, '').toUpperCase();
            }

            updateScanStatus(message, type = 'info') {
                if (!message) {
                    this.scanStatus.className = 'small text-muted mt-2 text-center';
                    this.scanStatus.innerHTML = '';
                    return;
                }

                const icon = type === 'success'
                    ? '<i class="fas fa-check text-success me-1"></i>'
                    : '<i class="fas fa-exclamation-circle text-danger me-1"></i>';

                const classes = type === 'success'
                    ? 'alert alert-success py-2 px-3 mt-2'
                    : 'alert alert-danger py-2 px-3 mt-2';

                this.scanStatus.className = `${classes} text-center`;
                this.scanStatus.innerHTML = `${icon}${message}`;
            }

            showStatus(message, type) {
                if (!message) {
                    this.statusMessage.innerHTML = '';
                    return;
                }

                let alertClass = 'alert-info';
                if (type === 'error') {
                    alertClass = 'alert-danger';
                } else if (type === 'success') {
                    alertClass = 'alert-success';
                }

                this.statusMessage.innerHTML = `
                    <div class="alert ${alertClass} mb-4" role="alert">
                        ${message}
                    </div>
                `;
            }

            showLoading(show) {
                if (show) {
                    this.loadingSpinner.style.display = 'inline-block';
                    this.submitBtn.disabled = true;
                } else {
                    this.loadingSpinner.style.display = 'none';
                    this.submitBtn.disabled = false;
                }
            }

            async processExit() {
                const rawPass = this.passInput.value.trim();

                if (!rawPass) {
                    this.passInput.classList.add('is-invalid');
                    this.showStatus('Please scan the visitor pass to confirm your exit.', 'error');
                    return;
                }

                const normalizedPass = this.normalizeCard(rawPass);
                this.passInput.value = normalizedPass;
                this.passInput.classList.remove('is-invalid');
                this.passInput.classList.add('is-valid');
                this.updateScanStatus(`Pass detected: <strong>${normalizedPass}</strong>`, 'success');
                this.showStatus('', null);
                this.showLoading(true);

                try {
                    const response = await fetch('/api/kiosk/visitor/exit', {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json'
                        },
                        body: JSON.stringify({
                            rfidCardUid: normalizedPass
                        })
                    });

                    const result = await response.json();

                    if (!response.ok) {
                        throw new Error(result.error || 'Failed to process exit');
                    }

                    this.showSuccessScreen(result.data.visitor);

                } catch (error) {
                    console.error('Exit error:', error);
                    this.showLoading(false);
                    this.passInput.classList.add('is-invalid');
                    this.updateScanStatus(error.message || 'Unable to locate active visitor for this pass.', 'error');
                    this.showStatus(error.message || 'Exit failed. Please contact staff for assistance.', 'error');
                    this.passInput.focus();
                }
            }

            startCountdown(seconds, callback) {
                let remaining = seconds;
                const countdownElement = document.getElementById('countdown');

                const tick = () => {
                    if (!countdownElement) {
                        return;
                    }

                    countdownElement.textContent = remaining;

                    if (remaining <= 0) {
                        callback();
                    } else {
                        remaining -= 1;
                        setTimeout(tick, 1000);
                    }
                };

                tick();
            }

            showSuccessScreen(visitor) {
                this.showLoading(false);

                const kioskBody = document.querySelector('.kiosk-body');
                const formFooter = document.querySelector('.form-footer');

                const exitTime = new Date(visitor.exitTime);
                const entryTime = new Date(visitor.entryTime);
                const durationMinutes = visitor.durationMinutes ?? Math.round((exitTime - entryTime) / 60000);

                kioskBody.innerHTML = `
                    <div class="text-center py-4">
                        <i class="fas fa-door-open text-success" style="font-size: 4rem;"></i>
                        <h2 class="mt-3 text-success">Exit Confirmed</h2>
                        <p class="lead">Safe travels, ${visitor.name}!</p>
                        <div class="card shadow-sm mt-4">
                            <div class="card-body text-start">
                                <p class="mb-2"><strong>Visitor ID:</strong> ${visitor.id}</p>
                                <p class="mb-2"><strong>Visitor Pass:</strong> ${visitor.rfidCardUid}</p>
                                <p class="mb-2"><strong>Purpose:</strong> ${visitor.purpose}</p>
                                <p class="mb-2"><strong>Entry Time:</strong> ${entryTime.toLocaleString()}</p>
                                <p class="mb-0"><strong>Exit Time:</strong> ${exitTime.toLocaleString()}</p>
                            </div>
                        </div>
                        <div class="badge bg-primary fs-6 mt-4 px-3 py-2">
                            <i class="fas fa-stopwatch me-2"></i>
                            Visit Duration: ${durationMinutes} min
                        </div>
                        <div class="alert alert-info mt-4">
                            <i class="fas fa-clock me-2"></i>
                            Returning to the main menu in <strong id="countdown">7</strong> seconds...
                        </div>
                    </div>
                `;

                formFooter.innerHTML = `
                    <button type="button" class="btn btn-primary btn-kiosk" onclick="window.location.href='/kiosk/'">
                        <i class="fas fa-home"></i> Return to Main Menu
                    </button>
                `;

                this.startCountdown(7, () => {
                    window.location.href = '/kiosk/';
                });
            }
        }

        document.addEventListener('DOMContentLoaded', () => {
            window.visitorExitProcessor = new VisitorExitProcessor();
        });
    </script>
</body>
</html>
